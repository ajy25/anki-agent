"""Anki Agent — keyword/AI search and a cited tutor over an Anki deck.

The Flask app lives in `ankiagent.app`; build the index with `ankiagent.ingest`.
Entry points are at the project root: `run.py` (browser) and `launcher.py`
(desktop hotkey window).
"""
